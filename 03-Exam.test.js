//Funcion substract (a,b)  añadir una funcion de utilidades matematicas y probarlas
//agrega una funcion substract(a,b) que devuelva la resta de a-b asegurate de exportarala conmodule.exports
//Escribe al menos 2 pruebas
//una para substract(10,3) que debe dar 7
//y otra de substract (3-10) que debe de dar -7

const {average} = require('./03-Exam.js');

test('adds 10 - 3 to equal ', () => {
  const result = average(10, 3);
  expect(result).toBe();
}
);

const {average} = require('./03-Exam.js');

test('adds 3 - 10 to equal ', () => {
  const result = average(3, 10);
  expect(result).toBe();
}
);


//